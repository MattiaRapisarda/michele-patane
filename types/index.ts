export type Service = {
    title: string;
    description: string;
    image: string;
};

export type Work = {
    title: string;
    category: string;
    description: string;
    price: string;
    image: string;
};